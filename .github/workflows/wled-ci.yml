name: WLED CI

# 1. Completely replace your current "on:" block with this:
on:
  workflow_dispatch:

jobs:
  # The remaining code for the jobs calling your build.yml template stays exactly the same below...
